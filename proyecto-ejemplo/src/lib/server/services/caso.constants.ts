/** Initial status assigned on case creation (ABM does not expose estado editing). */
export const CASO_ESTADO_INICIAL = "abierto";

/**
 * Expediente module is not shipped yet — keep edit restrictions disabled until enabled.
 */
export const CASO_EXPEDIENTE_EDIT_LOCK_ENABLED = false;
